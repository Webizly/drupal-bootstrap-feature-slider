<?php

/**
 * Implements hook_block_info().
 *
 */
function banner_feature_block_info() {
  $blocks = array();
  $blocks['bannerfeature'] = array(
    'info' => t('Banner Feature Module Block'), //Module name
    'cache' => DRUPAL_CACHE_PER_ROLE,
  );
  return $blocks;
}
